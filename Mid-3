#include <stdio.h>

#include <string.h>

int main(){

    char string[21];

    printf("Enter a string : ");

    scanf(" %s", &string);

    if (strlen(string) > 20) {

        printf("THe length of the strnig must be less than 20 characters.");

        return 0;

    }

    printf("The reversed string is %s", strrev(string));

    return 0;

} 

